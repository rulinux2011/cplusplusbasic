#include <stdio.h>

int main()
{
int p1[] = {1, 3, 5, 7 ,9};
int *p = p1;

*p++;
printf("*p++ = %d\n", *p);//结果为3 （++优先级比*高）先p++后p指针此时指向p1[1]=3出，再*P取值操作即把3取出

int p2[] = {1, 3, 5, 7 ,9};
p = p2;

(*p)++;
printf("(*p)++ = %d\n", *p);//结果为2 先取值操作此时*p=1 然后再++操作即执行1+1=2 即为最后结果

return 0;
}
